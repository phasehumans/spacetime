# Regex Replace (011-regex-replace)

## Description
Use sed to replace a string across multiple files.

## Prompt Given to Agent
```text
Change 'db_host=localhost' to 'db_host=db.internal' in all .conf files in the /configs directory."
```

## Base Image
`ubuntu:22.04`
