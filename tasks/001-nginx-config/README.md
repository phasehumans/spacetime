# Fix Nginx Configuration (001-nginx-config)

## Description
The agent needs to find and fix a syntax error in the nginx config and restart the service.

## Prompt Given to Agent
```text
The Nginx server is failing to start due to a syntax error. Please identify the issue in the default site configuration, fix it, and ensure the service can start successfully."
```

## Base Image
`ubuntu:22.04`
