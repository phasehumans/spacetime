# Fix Permissions (012-fix-permissions)

## Description
Fix file permissions so a script can run.

## Prompt Given to Agent
```text
The script at /usr/local/bin/runapp is throwing a permission denied error. Fix its permissions so it is executable by anyone."
```

## Base Image
`alpine:latest`
