# Tar Extraction (010-tar-extraction)

## Description
Extract a specific file from a tar archive.

## Prompt Given to Agent
```text
There is an archive at /data.tar.gz. Extract ONLY the file 'secret.txt' from it and place it at /root/secret.txt."
```

## Base Image
`alpine:latest`
