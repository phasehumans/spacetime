# SQLite Query (016-sqlite-query)

## Description
Query a SQLite database.

## Prompt Given to Agent
```text
Query the SQLite database at /data.db to find the username with id=1, and save the username to /tmp/admin.txt."
```

## Base Image
`ubuntu:22.04`
