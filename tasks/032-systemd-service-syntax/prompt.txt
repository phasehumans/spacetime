The systemd service unit /etc/systemd/system/metric-collector.service has invalid unit syntax, an incorrect relative ExecStart path, and missing section headers. Inspect and fix the unit file so that it points to absolute binary path /usr/local/bin/collector.sh, has valid [Unit], [Service], and [Install] sections with Restart=always and WantedBy=multi-user.target, and ensure the target script is executable.
