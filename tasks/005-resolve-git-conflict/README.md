# Resolve Git Conflict (005-resolve-git-conflict)

## Description
Resolve a simulated git merge conflict.

## Prompt Given to Agent
```text
There is a git repository at /repo with a merge conflict in file.txt. Resolve the conflict by keeping the 'master' version, and complete the merge commit."
```

## Base Image
`alpine:latest`
