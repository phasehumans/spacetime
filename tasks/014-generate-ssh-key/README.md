# Generate SSH Key (014-generate-ssh-key)

## Description
Generate an SSH keypair.

## Prompt Given to Agent
```text
Generate an RSA SSH keypair at /root/.ssh/id_rsa with no passphrase."
```

## Base Image
`ubuntu:22.04`
