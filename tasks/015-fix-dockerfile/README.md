# Fix Dockerfile (015-fix-dockerfile)

## Description
Fix a simulated Dockerfile syntax error.

## Prompt Given to Agent
```text
The Dockerfile at /project/Dockerfile has a typo causing builds to fail. Fix the typo."
```

## Base Image
`ubuntu:22.04`
